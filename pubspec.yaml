name: finance_tracker
description: A lightweight Finance Tracker application

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shimmer: ^3.0.0
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  http: ^0.13.5
  sqflite: ^2.2.8
  path_provider: ^2.0.15
  intl: ^0.18.1
  pdf: ^3.10.4
  get_it: ^7.2.0
  flutter_svg: ^1.1.6
  cached_network_image: ^3.2.3
  path: ^1.8.3

dev_dependencies:
  shimmer: ^3.0.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.4.0
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
